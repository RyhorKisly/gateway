package io.ylab.userservice.core.enums;

/**
 * Enum representing types of errors.
 * <p>
 * This enum defines different types of errors, such as structured errors and general errors.
 * It serves as a classification for different error types within the system.
 */
public enum ErrorType {
    STRUCTURED_ERROR, ERROR;

    private ErrorType() {
    }
}
