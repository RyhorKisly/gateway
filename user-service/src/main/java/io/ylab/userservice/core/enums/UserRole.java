package io.ylab.userservice.core.enums;

/**
 * Enum representing roles for users in the system.
 * <p>
 * This enum defines different roles that users can have, such as regular users, experts, and administrators.
 * It provides a classification for different user roles within the system.
 */
public enum UserRole {
    USER, EXPERT, ADMIN
}
